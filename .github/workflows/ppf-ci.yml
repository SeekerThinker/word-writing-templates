name: PPF continuous-web validation
on:
  pull_request:
  push:
    branches: [main]
permissions:
  contents: read
jobs:
  web-gate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Validate full publication gate
        run: make web-publish-check
