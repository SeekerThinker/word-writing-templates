# 稳定版承诺

从 **v3.0.0** 起，本项目进入稳定版阶段。目标不是继续增加模板数量，而是让已经依赖这些模板的普通用户、链接和自动化入口可以长期放心使用。

## 核心目的也属于稳定范围

这个项目首先是一个**结构化思考与写作工具**，其次才是排版模板。

标题层级用于把想法分组、命名和建立从属关系；Word 导航窗格用于持续查看全文结构，并在需要时调整章节顺序。自动编号、目录、页码、题注、脚注和参考文献等能力，主要负责减少格式维护对思考和写作的打断。

因此，3.x 的产品改进应继续围绕这个顺序：

**思考 → 结构 → 写作 → 排版**

新功能如果只是增加格式选项，却明显增加普通用户的选择成本，默认不应进入核心路径。

## 3.x 稳定范围

在整个 3.x 系列中，除非发现无法通过兼容方式修复的严重问题，否则以下内容保持稳定：

- 产品矩阵维持 **6 种编号 × 2 种起步方式 × 2 个平台 = 24 个 `.dotx`**；
- Windows / macOS 的 6 个“直接开始版”中文文件名保持不变；
- 对应的 6 个“带常用结构版”中文文件名保持不变；
- `templates/<platform>/books/`、`templates/<platform>/articles/` 与 `templates/<platform>/structured/...` 路径保持可用；
- 两个稳定整包下载地址保持不变：
  - `releases/latest/download/Word-Writing-Templates-Windows.zip`
  - `releases/latest/download/Word-Writing-Templates-macOS.zip`
- 普通用户模板继续使用 `.dotx`，不改成需要启用宏的 `.dotm`；
- 不把 Python、GitHub、插件、VBA 或命令行变成普通用户的运行依赖；
- “下载 → 解压 → 双击 `.dotx` → Word 新建文档”继续作为默认使用路径；
- 标题层级与导航窗格继续作为长文结构化思考的核心使用方式，不把它隐藏在纯排版功能之后。

## 什么可以在 3.x 中改进

以下变化通常属于兼容更新，可以在 3.x 小版本中发布：

- 修复 Word / macOS / Windows 兼容问题；
- 调整不会破坏既有文档结构的样式细节；
- 改进标题层级、导航窗格和长文重组相关的说明与使用体验；
- 改进自动校验、渲染测试、发布流程和文档；
- 增强真实 Word 验收与反馈记录；
- 修复题注、脚注、页眉页码、打印版式等已有功能的问题；
- 增加不要求用户重新选模板的辅助能力。

## 哪些变化需要新的主版本

下面这些变化如果确实需要实施，应优先留到未来的主版本，而不是悄悄塞进 3.x：

- 删除或重命名现有编号方案；
- 改变稳定模板文件名或目录路径；
- 改变稳定 Release 资产名；
- 让普通用户必须安装插件、运行脚本或启用宏；
- 默认改变现有模板的核心编号体系；
- 大规模改变结构版的分页 / 分节逻辑，导致旧教程或工作流明显失效；
- 把项目从“结构化思考与写作”转成以格式选项数量为中心的产品。

## 升级策略

`.dotx` 是“新建文档的模板”，不是已经写好的 `.docx` 文稿的运行依赖。因此：

- 已经创建并保存成 `.docx` 的文稿，不需要为了模板升级而重新套模板；
- 新版本主要影响以后从模板创建的新文档；
- 如果某个版本只修复文档、CI 或下载体验，而模板内容本身没有变化，用户通常没有必要重新下载；
- 如果某个修复确实建议用户重新下载，Release 说明会明确写出原因。

## 兼容性声明边界

项目会在 Windows、macOS 和 Linux runner 上做 OOXML 结构检查，并在主构建中把全部 24 个模板渲染为 PDF 做布局冒烟测试。

这些检查不能替代真实 Microsoft Word。只有存在公开、可追踪的 Windows / Mac 真机反馈时，项目才会把对应 Word 版本记为实际验证结果。

## 维护原则

稳定版优先级依次是：

1. 帮助用户保持思路结构清晰、可见、可调整；
2. 不破坏普通用户现有下载与使用路径；
3. 不破坏模板文件名、编号体系和无宏体验；
4. 修复真实兼容问题；
5. 提升自动测试和可维护性；
6. 最后才考虑新增功能。

如果一个新功能会显著增加普通用户的选择成本，它默认不应该进入稳定版核心路径。
