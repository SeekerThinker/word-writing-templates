# 稳定版承诺

从 **v4.0.0** 起，本项目把“一个编号方案只对应一个用户模板”作为新的稳定产品基线。

v3.x 的 24 模板矩阵已经完成其历史任务，并继续保留在对应 GitHub Release 中；v4 不要求任何已经写成 `.docx` 的文档迁移或重新套模板。

## 核心目的保持不变

这个项目首先是一个**结构化思考与记录 / 写作工具**，其次才是排版模板。

标题层级帮助用户把想法、笔记、问题和材料分组、命名、排序并建立关系；Word 导航窗格让这些层级持续可见，便于检查整体结构并重新组织内容。排版能力的任务是减少摩擦，让注意力尽量留在思考和内容上。

**整理思路、积累笔记本身就是完整用途；成稿是可选延伸，不是必然终点。** 如果以后需要把内容发展成文章、报告或书稿，同一套标题结构可以继续承担导航、编号、目录和正文组织。

因此产品设计继续遵循：**思考 → 结构 → 记录 / 写作 → 排版。**

## 为什么 v4 合并两种起步方式

v3 同一个编号方案同时提供“直接开始版”和“带常用结构版”。实际使用中，前言、摘要、作者信息、目录、参考文献等可选成稿区块并不会妨碍结构化思考：暂时不用可以保留，需要时填写，不需要时删除。

v4 因此只保留一个用户模板。打开后可以直接使用 `标题 1 / 标题 2 / 标题 3 / 标题 4` 和正文组织内容；前言、目录、摘要、参考文献、页码、题注等能力已经预置，需要时再使用即可。

这些可选成稿区块使用非大纲样式，不进入 Word 导航窗格；导航窗格仍以真正的标题层级为主，避免辅助结构干扰思考结构。

## 4.x 稳定范围

在整个 4.x 系列中，除非发现无法通过兼容方式修复的严重问题，否则以下内容保持稳定：

- 用户产品矩阵为 **6 种编号 × 2 个平台 = 12 个 `.dotx`**；
- Windows / macOS 各提供 3 个书籍模板和 3 个文章模板；
- 公共模板文件名保持不变，例如 `书籍-中文传统.dotx`、`文章-中文论文.dotx`；
- 公共模板路径继续使用 `templates/<platform>/books/` 与 `templates/<platform>/articles/`；
- 两个稳定整包下载地址保持不变：
  - `releases/latest/download/Word-Writing-Templates-Windows.zip`
  - `releases/latest/download/Word-Writing-Templates-macOS.zip`
- 普通用户模板继续使用无宏 `.dotx`；
- 不把 Python、GitHub、插件、VBA 或命令行变成普通用户的运行依赖；
- “下载 → 解压 → 双击 `.dotx` → Word 新建文档”继续作为默认使用路径；
- 标题层级与导航窗格继续作为核心思考工作流；
- 项目文案不把“最终写成文章 / 成稿”描述为所有使用场景的必然终点。

仓库在 v4 初期可能仍保留 v3 的 `templates/<platform>/structured/...` 生成副本作为内部兼容构建产物；它们不再属于 v4 的用户产品矩阵，也不进入 Release ZIP，4.x 不承诺这些内部路径长期存在。

## 什么可以在 4.x 中改进

以下变化通常属于兼容更新：

- 修复 Windows / macOS Word 兼容问题；
- 调整不会破坏既有标题层级的样式细节；
- 让可选成稿区块更安静、更容易保留或删除；
- 改进导航窗格、目录、页码、题注、脚注和参考文献体验；
- 改进模板对日常整理、长期积累和正式写作之间切换的支持；
- 改进自动校验、渲染测试和发布流程。

## 哪些变化需要新的主版本

下面这些变化不应悄悄进入 4.x：

- 删除或重命名现有 6 种编号方案；
- 改变稳定用户模板文件名或公共路径；
- 改变两个稳定 Release 资产名；
- 让普通用户必须安装插件、运行脚本或启用宏；
- 默认改变现有模板的核心编号体系；
- 大规模改变书籍分节 / 页码逻辑，导致既有教程或工作流明显失效。

## 升级策略

`.dotx` 是“新建文档的模板”，不是已经保存好的 `.docx` 文档的运行依赖。因此：

- 已经创建并保存成 `.docx` 的文档，不需要为了模板升级而重新套模板；
- v3 用户如果喜欢原来的两套起步方式，可以继续使用 v3.0.1 Release；
- v4 主要影响以后从模板创建的新文档；
- 如果某个修复确实建议用户重新下载，Release 说明会明确写出原因。

## 兼容性声明边界

项目会在 Windows、macOS 和 Linux runner 上做 OOXML 结构检查，并在主构建中对生成模板做 PDF 布局冒烟测试。

这些检查不能替代真实 Microsoft Word。项目不会把自动结构检查或 LibreOffice 渲染结果表述为 Microsoft Word 真机验证。

## 维护原则

稳定版优先级依次是：

1. 保持“结构化思考优先”的产品目的；
2. 承认整理、积累本身就是完整用途，不预设必须成稿；
3. 减少普通用户不必要的选择；
4. 保持标题层级与导航窗格的思考结构清晰；
5. 不破坏稳定下载、文件名和无宏体验；
6. 修复真实兼容问题；
7. 最后才考虑新增功能。

如果一个新功能会显著增加普通用户的选择成本，却不能明显改善思考、记录或写作，它默认不应该进入核心路径。
