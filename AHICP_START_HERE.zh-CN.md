# AHICP 接管入口

本仓库采用 AHICP `research-full`、完整 PPF 与 Vault Interface。本次升级只改变长期协作/出版基础设施，不修改既有项目内容。

先读 `AGENTS.md`，再读 AHICP manifest/context、Working Memory/Decision Log、Core/Map/Status 适配文件，最后读取 `project-stack.yaml` 与 `publishing.yaml`。实质性内容工作前使用 `docs/ahicp/ONBOARDING_REPORT_TEMPLATE.md`。
