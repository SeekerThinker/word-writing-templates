# Inquiry Publishing Stack — local adoption

This repository is a downstream project. Exact revisions are in `project-stack.yaml`. AHICP governs durable AI collaboration, PPF the publication lifecycle, Vault Interface portable metadata, and Starter composition/upgrades.

Fresh-read pinned upstream manifests before future upgrades. Functionally map reliable existing files and never auto-overwrite project-owned content.
