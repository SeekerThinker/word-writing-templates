# Vault Interface Handoff

This repository uses the Vault Interface as an additive project contract. Existing project files remain authoritative for project content. Agents must preserve provenance, distinguish proposals from approvals, and record external publication state separately from source state.
