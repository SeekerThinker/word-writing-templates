# 真实 Microsoft Word 验收清单

这份清单给愿意帮忙做 **Windows / Mac 真机测试** 的普通用户使用。不需要懂 GitHub、OOXML 或 Word 排版原理。

> 自动化测试已经检查模板结构；这里要验证的是“在真正的 Microsoft Word 里，普通用户是否真的顺手”。

## 你可以选两种反馈方式

### A. 只想说“我用过，没问题”

如果你已经在 Microsoft Word 桌面版里实际用过模板，而且暂时没有发现明显问题，可以直接提交这个几十秒表单：

**[提交轻量正常反馈](https://github.com/SeekerThinker/word-writing-templates/issues/new?template=word_quick_success.yml)**

这种反馈会单独标记为“轻量正常反馈”，不会被当成完整兼容性验证。

### B. 做一次 3～5 分钟完整验证

如果愿意多花几分钟，请按下面的清单测试，然后提交：

**[提交完整真机验证](https://github.com/SeekerThinker/word-writing-templates/issues/new?template=word_compatibility_report.yml)**

新版表单会直接让你逐项选择“正常 / 有问题 / 未测试”，不需要自己整理格式。

## 开始前

1. 从 [最新版 Release](https://github.com/SeekerThinker/word-writing-templates/releases/latest) 下载与你电脑对应的 ZIP。
2. 解压后任选一个 `.dotx` 模板，并记住自己测试的是哪一种：
   - **直接开始版**：位于“书籍模板 / 文章模板”文件夹第一层；
   - **带常用结构版**：位于其中的“带常用结构”子文件夹。
3. 第一次测试推荐使用“中文传统 / 中文论文”编号：
   - 写书：`书籍-中文传统.dotx` 或 `书籍-中文传统-常用结构.dotx`；
   - 写文章：`文章-中文论文.dotx` 或 `文章-中文论文-常用结构.dotx`。
4. 双击模板，用 **Microsoft Word 桌面版** 打开。

## 建议检查这些

- **能打开**：双击 `.dotx` 后，Word 创建一篇新的文档，而不是直接修改模板文件。
- **标题编号正常**：把一级标题改成自己的文字，编号仍然存在。
- **多级标题正常**：分别添加“标题 1 / 标题 2 / 标题 3 / 标题 4”，编号层级看起来正确。
- **正文正常**：正文能正常输入，字体、字号和行距没有明显异常。
- **保存再打开正常**：保存为普通 `.docx`，关闭 Word，再重新打开，标题和编号没有坏掉。
- **目录正常（可选）**：插入目录后，能看到各级标题；修改标题后“更新目录 / 更新域”能刷新。测试书籍常用结构版时，建议特别检查它已经预置的目录能否正常更新。
- **页码 / 页眉正常（结构版，可选）**：书籍版的书名页不显示页码，前言 / 目录显示 `i, ii, iii…`，正文从 `1` 重新开始；文章版从第 `1` 页开始显示页码。
- **v2.7 书籍分页（结构版，可选）**：在正文后新建一个“标题 1”，确认它自动从新页开始；不需要手工插入分页符。
- **v2.7 奇偶页版式（书籍结构版，可选）**：在打印布局中翻到正文的奇数页和偶数页，确认奇数页页眉显示当前章节、偶数页页眉显示书名；页码分别位于页面外侧（奇数页靠右、偶数页靠左）。
- **v2.7 双面打印边距（书籍结构版，可选）**：打开“布局 / 页边距”或打印预览，确认文档使用镜像页边距并为装订预留了额外空间。如果你的 Word 菜单显示方式不同，只要左右页的内侧边距有装订余量即可。
- **v2.8 图表题注（可选）**：插入一个图片或表格，用“引用 → 插入题注”，确认可以选择“图 / 表”标签；书籍模板应按章编号，文章模板应连续编号。
- **v2.8 交叉引用（可选）**：在正文中插入对刚才图表的交叉引用，增删前面的图表后更新域，确认引用编号能跟着变化。
- **v2.9 脚注 / 尾注（可选）**：插入一条脚注或尾注，确认注释正文为较小的正文字体、编号为上标，保存重开后仍正常。
- **v2.9 参考文献条目（可选）**：在样式区域选择“参考文献条目”，输入一条足够长、能换到第二行的文献，确认第二行出现悬挂缩进。
- **引用管理器（使用者可选）**：如果你本来使用 Zotero / EndNote，可以插入一条引用并刷新参考文献，确认插件仍能正常更新字段。没有安装这些工具完全不需要测试这一项。
- **常用结构正常（仅结构版）**：书籍版能看到书名页、前言、目录、附录、参考文献；文章版能看到作者、单位、日期、摘要、关键词、参考文献，而且删除不需要的区块后正文仍正常。
- **导航窗格正常（可选）**：标题能出现在导航窗格中。
- **快捷键正常（可选）**：至少试一个标题快捷键和正文快捷键；如果被系统、输入法或插件占用，也请如实记录。

> 书籍结构版不会强制“每章必须从奇数页 / 右页开始”。如果出版社明确要求右页起章，那属于最终出版排版规则，不应把普通的“自动另起新页”误判为故障。

## 快捷键参考

| 用途 | Windows | macOS |
|---|---|---|
| 一级标题 | `Ctrl + Alt + 1` | `⌘ + ⌥ + 1` |
| 二级标题 | `Ctrl + Alt + 2` | `⌘ + ⌥ + 2` |
| 三级标题 | `Ctrl + Alt + 3` | `⌘ + ⌥ + 3` |
| 四级标题 | `Ctrl + Alt + 4` | `⌘ + ⌥ + 4` |
| 正文 | `Ctrl + Alt + Z` | `⌘ + ⌥ + Z` |
| 表格文字 | `Ctrl + Alt + B` | `⌘ + ⌥ + B` |
| 引用 / 特别内容 | `Ctrl + Alt + Q` | `⌘ + ⌥ + Q` |
| 插入脚注 | `Ctrl + Alt + F` | `⌘ + ⌥ + F` |

快捷键不是模板正常工作的前提。某个快捷键没反应时，请再从 Word 的“样式”区域试一次对应标题或正文；这样可以区分“模板结构问题”和“快捷键冲突”。

## 隐私

不需要提供个人信息，也不需要上传你的真实写作内容。截图是可选的；如果要截图，请只保留能说明问题的 Word 界面部分。