# Changelog

## 3.1.0 - 2026-09-14

- 把项目的核心定位明确为“结构化思考与写作”：标题层级首先用于帮助用户分组、命名、排序和检查思路，排版自动化用于减少技术细节对思考的打断。
- README 第一屏改为“让思路有层级，让结构可调整，让想法直接长成文章”，并明确“模板不会替你思考，但会让思考的结构显现出来”。
- 新增 `docs/结构化思考.md`，说明如何使用 Word 标题层级和导航窗格观察全文结构、拖动章节、边写边重构，以及为什么不必先把大纲一次想完。
- `docs/快速开始.md` 把导航窗格从“长文以后再学的附加功能”提升为最值得尽早掌握的核心能力，并给出“写内容 → 建层级 → 看结构 → 调整 → 继续写”的简单路径。
- `STABILITY.md` 把“思考 → 结构 → 写作 → 排版”写入 3.x 稳定原则，避免未来版本重新滑回以格式选项数量为中心的产品方向。
- 用户下载包中的 `00-先看这里.txt` 增加结构化思考与导航窗格提示；Release 说明同步强调 v3.1 是价值定位与使用体验升级，不改变 24 个模板的文件名、编号体系和稳定下载地址。
- 不增加模板数量，不改变现有 `.dotx` 核心排版与编号行为；已经使用 v3.0 模板写作的用户不需要为了 v3.1 重新套用模板。

## 3.0.0 - 2026-09-14

- 项目进入稳定版阶段；3.x 优先保持现有 24 个 `.dotx` 产品矩阵、中文文件名、公开模板路径与两个 `releases/latest` 整包下载地址稳定。
- 新增根目录 `STABILITY.md`，明确 3.x 的兼容承诺、允许的小版本改进、需要留到未来主版本的破坏性变化，以及旧 `.docx` 文稿不需要“重新套模板”的升级原则。
- README 重新收口为稳定版入口：保留在线选择向导、6 种编号预览和零基础下载路径，同时统一说明成稿版式、图表题注 / 交叉引用、脚注 / 尾注与参考文献能力，移除面向用户的旧版本号叙述。
- 新增 `scripts/stability_check.py`，保护 24 个稳定模板路径、稳定下载链接与稳定版说明；跨平台结构检查会同时执行这套契约检查。
- 新增 `scripts/release_audit.py`，在正式发布前检查 Windows / macOS 用户 ZIP：每个平台必须恰好包含 12 个 `.dotx`、快速入门文件，并拒绝宏模板、安装程序、VBA 载荷或意外文件。
- 主 Release 工作流加入稳定接口检查和用户 ZIP 发布审计，只有生成、结构校验、24 模板 PDF 布局冒烟、打包与发布包审计全部通过后才创建 / 刷新 Release。
- 更新 `MAINTAINERS.md`、`CONTRIBUTING.md` 与兼容性文档，统一 24 模板、本地检查命令、真实 Word 验收边界和 3.x 版本策略。
- 清理早期上传绕过方案遗留的 `scripts/build.py.b64`，正式构建只保留可读、可测试的标准 Python 脚本。
- v3.0 不增加新的模板选择，也不改变既有标题编号、成稿版式、题注、脚注或参考文献工作流；重点是把 v2.x 已形成的能力固定成可长期维护的稳定基线。

## 2.9.0 - 2026-09-13

- 继续保持 24 个 `.dotx` 产品矩阵不变，不增加新的模板选择；本次集中整理脚注、尾注和参考文献工作流。
- 统一 Word 原生 `Footnote Text / Footnote Reference` 与 `Endnote Text / Endnote Reference` 样式：使用平台对应中文正文字体，注释正文默认 9 pt、单倍行距，注释编号保持上标。
- 新增 / 整理 Word 内置 `Bibliography` 样式，并新增面向普通用户的“参考文献条目”样式；两者默认使用约 21 pt 的悬挂缩进，长文献换行时更容易识别条目起点。
- 不强制脚注按页或按章重新编号，也不替用户决定作者—年份、数字编号、GB/T、APA 等具体引用规范；这些继续由 Word、学校 / 期刊规范或引用管理器决定。
- 明确 Zotero / EndNote 等工具的兼容边界：模板不内置引用插件、不改写第三方引用字段；引用管理器仍负责正文引用、排序、标点与最终输出样式。
- `scripts/reference_support.py` 扩展为统一处理题注、脚注 / 尾注和参考文献样式；`scripts/reference_check.py` 新增注释样式、上标、平台字体、悬挂缩进和文档保护检查。
- 下载包的 `00-先看这里.txt` 与快速开始 PDF 增加脚注 / 参考文献提示；新增 `docs/脚注与参考文献.md`，说明原生 Word 注释、手工文献以及 Zotero / EndNote 共存方式。
- 本地制作并渲染脚注 / 尾注 / 参考文献样式样例，重点检查中英文混排、悬挂缩进和页面布局；正式 Release 构建仍会渲染全部 24 个模板做布局冒烟检查。

## 2.8.0 - 2026-09-13

- 保持 24 个 `.dotx` 产品矩阵不变，为全部模板加入 Microsoft Word 原生图表题注与交叉引用支持，不引入宏、插件或新的用户选项。
- 模板在 `settings.xml` 中预置中文题注标签“图”和“表”：图默认放在对象下方，表默认放在对象上方。
- 书籍模板的图表题注默认跟随“标题 1”按章编号，并使用连字符作为章节号与图表序号的分隔符；不同章节编号方案会自然沿用对应的一级标题编号显示方式。
- 文章模板使用全文连续的十进制图号 / 表号，更适合论文、报告和一般长文章。
- 统一整理 Word 内置 `Caption` / “题注”样式：居中、黑色、正文同系列字体，并避免 Office 主题把题注显示成蓝色；新增“图表来源”样式用于数据来源 / 图片来源说明。
- 新增 `scripts/reference_support.py`，作为生成后的 OOXML 配置步骤；新增 `scripts/reference_check.py`，检查 24 个模板的题注标签、按章编号设置、题注样式、图表来源样式和字段更新设置。
- Windows / macOS / Linux 跨平台 CI 与正式 Release 构建都加入题注配置与验证步骤。
- 新增 `docs/图表与引用.md`，给普通用户说明“插入题注 → 交叉引用 → 更新域”的 Word 原生工作流，并明确无需手工维护图号 / 表号。
- 真机验收与兼容性反馈增加“题注 / 交叉引用”可选测试项；自动结构检查仍不会冒充 Microsoft Word 真机验证。
- 本地使用同一生成链重新验证全部 24 个模板，并额外渲染题注 / 图表来源样式示例及书籍 / 文章结构版，未发现截断、重叠或缺字。

## 2.7.0 - 2026-09-13

- 不增加任何模板选项，继续维持 24 个 `.dotx`；本次集中打磨“带常用结构版”的出版 / 打印细节。
- 书籍结构版启用镜像页边距并预留装订线，默认更适合双面打印；书名页、前置部分、正文三个节使用同一套打印页设置。
- 书籍结构版的“标题 1”默认自动另起新页，新增章节时不再需要手工插入分页符。
- 书籍正文启用奇偶页不同页眉：奇数页显示当前章节标题，偶数页显示书名；正文页码移动到页面外侧，前置部分罗马页码保持居中。
- 保留 v2.6 的独立书名页、前置部分罗马页码和正文从阿拉伯数字 `1` 重启的结构，不强制“每章必须从奇数页开始”，避免普通用户意外得到空白页。
- 文章结构版微调标题、作者、单位、日期与正文之间的间距，使第一页更接近可直接提交 / 打印的成稿节奏。
- 正文与常用文本样式加入孤行控制，标题样式加入“与下段同页”等分页保护，降低长文档中标题孤悬在页尾的概率。
- `scripts/manuscript_check.py` 扩展检查镜像页边距、装订线、奇偶页页眉 / 页脚、章节自动换页和对应 `STYLEREF` / `PAGE` 字段。
- 本地重新生成并渲染全部 24 个模板，共检查 42 个页面；Windows / macOS 两套版式均未发现截断、重叠或缺字。

## 2.6.0 - 2026-09-13

- 在不增加模板数量的前提下，把“带常用结构版”升级为更接近成稿的起点；仍维持 6 种编号 × 2 种起步方式 × 2 个平台，共 24 个 `.dotx`。
- 书籍结构版改为真实 Word 分节：独立书名页、前置部分、正文与后置部分三段结构。
- 书籍前置部分使用小写罗马页码 `i, ii, iii…`，正文从阿拉伯数字 `1` 重新开始，避免前言和目录页数变化后手工重排正文页码。
- 书籍正文新增自动书名页眉，使用标准 `STYLEREF "Title"` 域；页脚使用标准 `PAGE` 域。
- 文章结构版新增作者、单位、日期占位信息和独立样式，并从第 `1` 页开始显示页码。
- 新增 `scripts/manuscript_check.py`，自动检查分节、页码格式、页码重启、页眉字段、页脚字段以及文章元信息结构。
- Windows / macOS / Linux 跨平台 CI 均会重新生成模板并运行成稿结构检查；Release 构建仍会把全部 24 个模板转换为 PDF 做布局冒烟测试。
- 新增 `docs/成稿版式.md`，解释分页、页码、页眉、字段更新和兼容性边界。

## 2.5.0 - 2026-09-13

- 6 种编号方案全部新增“带常用结构版”，与原有“直接开始版”并行发布；Windows / macOS 合计 24 个 `.dotx` 成品模板。
- 书籍常用结构版预置前言、自动目录、正文起点、附录、参考文献；文章常用结构版预置摘要、关键词、正文、参考文献。
- 所有模板新增 `结构标题`、`目录标题`、`摘要`、`关键词`、`参考文献`、`模板提示` 等可复用样式，直接开始版写到一半也能继续扩展。
- 下载包保持零技术门槛：第一层仍是原来的直接开始模板；常用结构版收纳在“带常用结构”子文件夹，不增加新手第一步的选择负担。
- 书籍结构版目录使用 Word TOC 域，并开启打开文档时的字段更新提示；不需要的可选区块可直接删除。
- 自动验证从 12 个扩展到 24 个模板，并新增全部 24 个模板的 LibreOffice PDF 布局冒烟渲染。
- Windows / macOS / Linux 跨平台检查改为先从源码生成模板再验证，避免已提交成品与生成逻辑不同步。
- 真实 Word 反馈表单新增“直接开始版 / 带常用结构版”字段，公开兼容性矩阵也按起步版本区分记录。

## 2.4.0 - 2026-09-13

- 新增 `docs/真实Word验收.md`，普通用户可用 3～5 分钟完成 Windows / Mac 的 Microsoft Word 真机验收。
- 新增专用 `Word 真机兼容性反馈` Issue 模板，统一收集操作系统、Word 版本、模板名称和实际使用结果。
- 新增 `docs/兼容性验证记录.md`，明确区分自动 OOXML 结构检查与真实 Microsoft Word 验证。
- 兼容性文档加入“公开证据再标记已验证”的规则，避免把自动结构检查夸大成真机兼容性结论。
- 维护者文档加入真机兼容性 Issue 的记录与判定规范。
- README 增加真实 Word 验证入口，邀请普通用户在不提供个人写作内容的前提下参与兼容性验证。

## 2.3.0 - 2026-09-13

- 下载包新增更醒目的 `00-先看这里.txt`，把首次使用压缩为“选模板 → 双击 → 替换占位文字”。
- 新手说明补充“标题 / 一级标题 / 正文”三个最先需要认识的元素，以及导航窗格和目录的延后学习提示。
- 新增 `scripts/compatibility_check.py`，检查 12 个 `.dotx` 的 OOXML 包结构、四级编号、平台字体、快捷键映射、中文路径和宏文件缺失。
- 新增 Windows / macOS / Linux 三平台 GitHub Actions 结构兼容性检查。
- README 与维护文档明确自动化测试边界：结构检查不会伪装成 Microsoft Word 真机测试。
- Release 说明强化“零基础、下载即用、无需宏”的定位。

## 2.2.0 - 2026-09-13

- 新增零技术背景的在线模板选择向导。
- 向导按“写书/写文章 → 编号风格 → Windows/macOS”三步推荐并直接下载单个模板。
- 支持浏览器系统识别提示、可分享的选择链接和整包下载入口。
- Toolbox 的“Word 写作模板”入口改为优先打开选择向导。
- README 首页增加选择向导入口，进一步降低 GitHub 与 Word 排版知识门槛。

## 2.1.0 - 2026-09-13

- README 新增“30 秒开始”和 6 种模板的可视化选择区。
- 新增 Word 实际渲染预览图，以及 Windows / macOS 单独下载入口。
- 新增预览图自动生成脚本和稳定的 Release 下载文件名。

## 2.0.0 - 2026-09-13

- 从单一模板升级为“Word 结构化写作模板集”。
- 新增书籍与文章两大类，共 6 种常用编号方案。
- Windows 与 macOS 分别生成成品模板，共 12 个 `.dotx` 文件。
- 改为真正的 Word 模板格式：双击即创建新文档，无需手动复制原文件。
- Windows / macOS 分别优化常用中文字体。
- 统一快捷键：Windows 使用 `Ctrl+Alt`；macOS 使用 `Command+Option`。
- 引用快捷键使用 `Q`，降低常见快捷键冲突。
- 模板正文改为极简占位内容，教程移到 docs 与快速入门 PDF。
- 新增自动生成、校验和 GitHub Release 发布流程。
- 在 SeekerThinker/toolbox 中增加独立项目入口。

## 1.1.0

- 增加 Windows / macOS 跨平台快捷键方案。

## 1.0.0

- 从原始 Word 写作排版模板整理为开源项目。
